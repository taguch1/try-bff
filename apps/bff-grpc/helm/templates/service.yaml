apiVersion: v1
kind: Service
metadata:
  name: {{ include "bff-grpc.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "bff-grpc.name" . }}
    helm.sh/chart: {{ include "bff-grpc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 1323
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bff-grpc.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}

