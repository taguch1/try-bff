apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bff-grpc.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "bff-grpc.name" . }}
    helm.sh/chart: {{ include "bff-grpc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bff-grpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
